//Write a program to get two numbers and add them

#include<Stdio.h>
int main() {
    int a,b,sum;
    printf("Enter the value of a and b\n");
    scanf("%d",&a);
    scanf("%d",&b);
    sum=a+b;
    printf("The sum is : %d",sum);
    return 0;
}