//Write a program to calculate area of square

#include<Stdio.h>
int main() {
int side,area;
printf("Enter the side of square : "); 
scanf("%d",&side);
area=side*side;
printf("Area of square is : %d m^2",area);
    return 0;
}